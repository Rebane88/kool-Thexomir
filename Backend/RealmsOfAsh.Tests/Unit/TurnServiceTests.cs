namespace RealmsOfAsh.Tests.Unit;

/// <summary>
/// TurnService tests removed -- TurnService is now a gutted shell (NotImplementedException).
/// Phase 24 will rewrite the turn service with new tests.
/// </summary>
[Trait("Category", "Unit")]
public class TurnServiceTests
{
    // All tests removed: TurnService.EndTurnAsync now throws NotImplementedException.
}
