import { describe, it } from 'vitest';

describe('PhaseBanner', () => {
  it.todo('renders phase name with phase-specific color when phaseBannerPhase is set');
  it.todo('shows round number prominently');
  it.todo('auto-dismisses after timeout');
  it.todo('returns null when no phase banner is active');
});
