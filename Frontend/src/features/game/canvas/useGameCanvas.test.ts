import { describe, it } from 'vitest';

describe('useGameCanvas', () => {
  it.todo('returns setAnimating function');
  it.todo('RAF loop runs when animatingRef is true');
  it.todo('RAF loop runs when dirtyRef is true');
});
