import { describe, it } from 'vitest';

describe('useCountingTicker', () => {
  it.todo('returns target value immediately when no previous value');
  it.todo('interpolates from old value to new value over duration');
  it.todo('uses ease-out cubic easing');
  it.todo('cancels animation on unmount');
});
